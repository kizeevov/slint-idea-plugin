component App {
    TouchArea {
        pointer-event(e) => {
            if e.kind == PointerEventKind.down || e.kind == PointerEventKind.move {}
            if (e.kind == PointerEventKind.down || e.kind == PointerEventKind.move) {}
        }
    }
}